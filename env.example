# Upstash Redis Configuration
# Get these values from your Upstash Redis dashboard (https://console.upstash.com/)
UPSTASH_REDIS_REST_URL=https://your-redis-instance.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-redis-rest-token

API_KEY=custom-key

# Optional: Force extended thinking for supported models when requests omit it
# FORCE_THINKING_BUDGET=32000

# Optional: Force max_tokens to a fixed value on every request
# FORCE_MAX_TOKENS=64000

# Optional: Log incoming request summaries (no secrets)
# LOG_REQUEST_DEBUG=true

# Optional: ngrok settings for start.sh automation
# NGROK_ENABLED=true
# NGROK_DOMAIN=something-something-something.ngrok-free.dev
# NGROK_BIN=ngrok
# NGROK_CONFIG="$HOME/Library/Application Support/ngrok/ngrok.yml"

# Optional: Anthropic OAuth Client ID 
# Defaults to the official Claude CLI ID if not provided
# ANTHROPIC_OAUTH_CLIENT_ID=9d1c250a-e61b-44d9-88ed-5944d1962f5e

# Optional: Server port 
# Defaults to 9095 if not provided
# PORT=9095